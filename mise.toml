[tools]
lua = "latest"

[tasks.test]
run = "lua test/run.lua"

[tasks.install]
run = "sudo ln -sf /Users/davidlakubu/Code/Lua/llaunchd/bin/llaunchd /usr/local/bin/llaunchd"

[tasks.uninstall]
run = "sudo rm -f /usr/local/bin/llaunchd"

[tasks.list]
run = "bin/llaunchd list"

[tasks.help]
run = "bin/llaunchd help"
